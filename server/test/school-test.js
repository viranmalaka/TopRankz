/**
 * Created by malaka on 3/30/17.
 */
var schCtrl = require('../controllers/schoolController');
var assert = require('assert');
describe('School', function() {
  it('get all district should return ', function() {
    assert.equal(-1, [1,2,3].indexOf(4));
  });
});
